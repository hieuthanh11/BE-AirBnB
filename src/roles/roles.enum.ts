export enum Role_ENUM {
    ADMIN = 'ADMIN',
    HOST = 'HOST',
    CLIENT = 'CLIENT',
}

export enum Role_ENUM_USER {
    ADMIN = 'ADMIN',
    HOST = 'HOST',
    CLIENT = 'CLIENT',
    UNROLE = 'NO',
}
